## Summary
What this changes and why.

## Spec / source link
If this changes anything on the wire (header name, parameter, cookie, JWS field), link the spec section that motivates the change.

## Verification
- [ ] `npm run build` passes
- [ ] `npm test` passes
- [ ] If wire-format affected: ran `examples/express` against a real Chromium 146+ browser and confirmed `tier === "dbsc"` after registration

## Changelog
- [ ] Updated `CHANGELOG.md`
